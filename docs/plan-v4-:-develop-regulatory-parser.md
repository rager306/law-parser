# План v4: Разработка гибридного парсера НПА (LDUP/SALTP 2026)

Этот план является развитием v3 и полностью синхронизирован с архитектурными спецификациями LDUP (версии 2025-2026 гг.).

## 🧭 Когнитивная архитектура (SALTP 2026)

Реализуем **Self-Adaptive Legal Temporal Parser**, где DSPy 3.0.4 управляет рефлексивным ансамблем моделей.

### Слой 1: Perception (Восприятие)
*Цель: Формирование первичного структурированного образа документа.*
*   **DocumentIngester**: Мультиформатная загрузка (WordML 2003, DOCX, PDF, HTML).
*   **SourceDetector**: Определение источника (КонсультантПлюс, Гарант, pravo.gov.ru) для выбора набора правил (General vs Private YAML).
*   **GEPA 3.5 (Structural Bootstrap)**: Первичное выделение «скелета» документа (главы, статьи) на основе XML-паттернов и эмбеддингов структуры.
*   **SIMBA 2.2 (Morpho-Semantic Sensory)**: Первичное тегирование модальностей (обязанность, запрет) на уровне предложений.

### Слой 2: Understanding (Понимание)
*Цель: Глубокий семантический и темпоральный анализ.*
*   **MiPROv3 (Temporal Resolver)**: Построение bi-temporal модели и извлечение интервалов действия с использованием Temporal Memory v3 API.
*   **S-LLM Segmenter**: Доуточнение границ сложных сегментов (сноски, примечания, таблицы) с помощью Structure-Aware LLM.
*   **HCO Cache (Hybrid Context Optimization)**: **[КРИТИЧНО]** Кэширование семантических эмбеддингов между узлами для обеспечения 70-80% экономии токенов.

### Слой 3: Reasoning (Рассуждение)
*Цель: Построение графа знаний и выявление связей.*
*   **TCGR (Temporal Causal Graph Reasoner)**: Плагин для инференса причинных связей («поправка X изменила норму Y»).
*   **Cross-Reference Resolver**: Разрешение внутренних и внешних гиперссылок.
*   **GraphBuilder**: Формирование RDF*/JSON-LD триплетов с темпоральными ребрами.

### Слой 4: Learning (Обучение / Рефлексия)
*Цель: Обнаружение ошибок и генерация опыта.*
*   **SRC v2 (Reflexive Controllers)**: Группа контроллеров (Semantic, Temporal, Structural), генерирующих feedback в формате JSONL.
*   **Unified Feedback Queue**: Агрегация предложений по обновлению YAML-правил.

### Слой 5: Adaptation (Адаптация)
*Цель: Реконфигурация системы.*
*   **Policy Optimizer**: Reinforcement-движок, принимающий решения об активации новых правил на основе PolicyScore (ΔAccuracy, ΔLLM, ΔConflict).
*   **Neural Policy Tuning (NPT)**: Динамическая перенастройка маршрутов исполнения DSPy-графа в зависимости от типа и сложности акта.
*   **YAML Validator**: Эмпирическая проверка правил на корпусе документов (simulation mode).

### Слой 6: Action (Действие / Экспорт)
*Цель: Выдача стандартизированных результатов.*
*   **Exporters**: 
    - Akoma Ntoso (международный)
    - **LegalDocML-Russia** (государственный РФ)
    - LegalRuleML (логический)
    - NormML (ML-ориентированный)
*   **Knowledge Persistence**: Запись в FalkorDB + Graffiti Temporal Layer.

---

## 📂 Обновленная структура директорий

```
src_parser/
├── core/
│   ├── hco_cache.py            # [NEW] Модуль гибридного кэширования
│   ├── models.py               # Pydantic модели (SALTP compliant)
│   └── policy_npt.py           # [NEW] Neural Policy Tuning
├── perception/
│   ├── source_detector.py      # Детектор источника и Private YAML
│   └── preprocessors/          # Нормализаторы под форматы
├── understanding/
│   ├── gepa_v35.py             # Structural Bootstrap
│   ├── simba_v22.py            # Morpho-Semantic Analysis
│   └── miprov3_temporal.py     # Temporal Memory v3 Resolver
├── reasoning/
│   ├── tcgr_plugin.py          # Causal Reasoner Plugin
│   └── graph_builder.py        # RDF*/JSON-LD Generator
├── learning/
│   ├── src_controller_v2.py    # Reflexive Feedback
│   └── feedback_queue.py       # Unified JSONL Queue
├── adaptation/
│   ├── policy_optimizer.py     # Reinforcement Decision Engine
│   └── yaml_validator.py       # Corpus Simulation & Schema Check
├── exporters/
│   ├── akoma_ntoso.py
│   ├── legaldocml_ru.py        # [NEW] Российский стандарт
│   └── legal_ruleml.py
├── yaml_store/
│   ├── general/                # Универсальные паттерны
│   └── private/                # Паттерны источников (Консультант+)
└── pipeline_saltp.py           # Главный рефлексивный пайплайн
```

---

## 🚀 План реализации (Ближайшие шаги)

1.  **Infrastructure (UV + Python 3.13)**: Настройка окружения и установка DSPy 3.0.4.
2.  **YAML Base**: Создание `ldup_general.yaml` и `ldup_44fz.yaml` (Private) как декларативного фундамента.
3.  **Perception & HCO**: Реализация загрузчика WordML и **HCO Cache** (первоочередная задача для экономии бюджета токенов).
4.  **DSPy Workflow**: Сборка Standalone графа (GEPA -> SIMBA -> MiPROv3) без LangGraph.
5.  **Reflexive Loop**: Реализация SRC v2 и Policy Optimizer для автоматического дообучения на 44-ФЗ.
6.  **Action Layer**: Реализация экспорта в LegalDocML-Russia и FalkorDB.

---
**Критическое требование:** Никаких `as any` или `@ts-ignore` (в Python-эквиваленте — жесткая типизация). Все отклонения парсинга должны фиксироваться SRC-контроллером, а не подавляться в коде.
