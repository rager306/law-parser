**многоуровневая система: гибрид DSPy-LLM-Parser с token-эффективной логикой, динамическим самообучением и модульным стандартом совместимости**.

Ниже — полностью структурированная архитектура и требования к твоему парсеру,
уже **с учётом**:

* стандартов **Akoma Ntoso / LegalRuleML / MetaLex / LegalDocML-RU**,
* особенностей **DSPy 3.0.4**,
* и **архитектуры экономии токенов** (rule-first → LLM-assist → self-refine).

---

## 🧩 I. Цель и основная идея архитектуры

Создать **Legal Document Universal Parser (LDUP)** —
модульно-рефлексивный гибрид на **DSPy 3.0.4**,
который:

* 50–80 % задач решает **детерминированными правилами и алгоритмами** (без LLM);
* оставшиеся 20–50 % — **LLM-подсистемой** (для неформализуемых случаев);
* обучается по мере накопления примеров (через SRC Loop DSPy);
* поддерживает **многоформатные источники**, **многоязычные стандарты** и **многоуровневую структуру YAML-настроек**.

---

## ⚙️ II. Архитектура LDUP (DSPy 3.0.4 Hybrid Parser)

```mermaid
graph TD
    A[📥 Data Ingestion Layer] --> B[⚙️ Preprocessing & Format Normalizer]
    B --> C[🧠 DSPy 3.0.4 Hybrid Parsing Core]
    C --> D[🗃️ Knowledge Standards Adapter]
    D --> E[🧱 Output & Storage Layer]

    subgraph DSPy_Core[🧩 DSPy Core]
        C1[Rule-Based Structural Engine (GEPA 3.5)] --> C2[Morpho-Semantic Analyzer (SIMBA 2.2)]
        C2 --> C3[Temporal Resolver (MiPROv2 + Temporal Memory v3)]
        C3 --> C4[Causal Graph Engine (TCGR)]
        C4 --> C5[LLM-Assisted Layer (GPT / Mistral / Local)]
        C5 --> C6[SRC v2 Feedback Loop + HCO Cache]
    end

    subgraph Standards[📚 Legal Standards Adapter]
        D1[Akoma Ntoso Export] --> D2[LegalRuleML Mapping]
        D2 --> D3[MetaLex ID Integration]
        D3 --> D4[LegalDocML-RU + NormML Export]
    end

    subgraph Output[💾 Data Persistence]
        E1[FalkorDB + Graffiti Temporal Graph]
        E2[LangGraph Agent Interface]
    end
```

---

## 🧱 III. Слои архитектуры и их назначение

### 1️⃣ **Data Ingestion Layer**

* **Форматы:** XML 2003 (WordML), RTF, DOCX, PDF, HTML, TXT.
* **Источники:**

  * локальные файлы,
  * HTTP/HTTPS API,
  * интерактивные источники (через puppeteer-агент).
* **Функции:**

  * конвертация в внутренний XML-like DSL (LDML);
  * извлечение метаданных источника (продавец, версия, дата редакции).

> 💡 На этапе парсера запоминается `source_origin` (КонсультантПлюс, Гарант и т.д.), это важно для корректного паттерн-обучения.

---

### 2️⃣ **Preprocessing & Format Normalizer**

* Уравнивает все входные документы до универсального вида (псевдо-XML).
* Поддерживает lexical markup и style-based segment (для Word 2003).
* Результат → структурированные параграфы, ready для GEPA.

---

### 3️⃣ **DSPy Hybrid Parsing Core**

| Подмодуль                        | Реализация                           | Функция                                                   | Token Usage                     |
| -------------------------------- | ------------------------------------ | --------------------------------------------------------- | ------------------------------- |
| **GEPA 3.5**                     | deterministic + LLM prompt bootstrap | структурный анализ (глава, статья, пункт)                 | 🔵 ≈ 0 (LLM только в bootstrap) |
| **SIMBA 2.2**                    | rule + pymorphy + contextual LLM     | морфо-семантическая разметка (обязанность, запрет, право) | 🟡 10–20 %                      |
| **MiPROv2 + Temporal Memory v3** | hybrid regex + semantic              | извлечение дат и интервалов                               | 🔵 0                            |
| **TCGR plugin**                  | temporal-causal GNN                  | причинные связи поправок и редакций                       | 🟡 15 %                         |
| **LLM-Assist**                   | GPT-4 / Mistral / Llama3             | обработка аномалий и неформализованных фрагментов         | 🔴 20–30 %                      |
| **SRC v2 + HCO**                 | feedback / cache                     | самообучение и повторное использование эмбеддингов        | 🔵 – экономия до 70 %           |

---

### 4️⃣ **Knowledge Standards Adapter**

| Стандарт          | Что генерирует парсер                      | Где интегрируется    |
| ----------------- | ------------------------------------------ | -------------------- |
| **Akoma Ntoso**   | иерархия глав/статей/пунктов               | GEPA                 |
| **LegalRuleML**   | модальности норм, временные характеристики | SIMBA + MiPROv2      |
| **MetaLex**       | стабильные ID для узлов и редакций         | Graph Builder        |
| **LegalDocML-RU** | русская семантика + синонимы               | SIMBA                |
| **NormML**        | JSON для LLM дообучения (SRC)              | SRC / Feedback Layer |

---

### 5️⃣ **Output / Storage Layer**

* **FalkorDB + Graffiti:** временные и причинные графы.
* **LangGraph Interface:** LLM-агенты для вопросов и RAG.

---

## 🔋 IV. Архитектура экономии токенов (“Rule-First Design”)

| Этап                          | Тип решения               | Описание                                                               | Token Cost |
| ----------------------------- | ------------------------- | ---------------------------------------------------------------------- | ---------- |
| **Анализ структуры**          | Алгоритмический           | XML-паттерны + style-теги + регулярки                                  | 0          |
| **Морфология и семантика**    | Rule + Morph-model        | pymorphy3 + SIMBA pattern rules                                        | 0–1        |
| **Темпоральность**            | Hybrid (регекс + MiPROv2) | автоматическая нормализация дат                                        | 0          |
| **Кросс-ссылки и версии**     | Graph Traversal           | Link resolver                                                          | 0          |
| **Неформализуемые фрагменты** | LLM-Assist                | GPT / Mistral через policy-prompt (“describe non-standard clause”)     | 1          |
| **Self-Refine**               | SRC Loop                  | обучение на LLM результатах для снижения доли LLM в следующих запусках | –          |

🔸 **Цель:** LLM-слой активируется только для редких случаев (≈ 20 %),
а SRC постепенно снижает эту долю → до 10 %.

---

## 🧾 V. YAML-спецификации (универсальные и частные)

### 1️⃣ **Общая спецификация** (`ldup_general.yaml`)

```yaml
document:
  type: legal_act
  language: ru
  structure:
    - chapter
    - article
    - part
    - clause
  metadata:
    extract: [title, date, law_number, version]
  semantic:
    modalities: [obligation, permission, prohibition, definition, sanction]
  temporal:
    detect_patterns: ["вступает в силу", "утратил силу"]
  standards:
    export: [AkomaNtoso, LegalRuleML, MetaLex]
  source_tracking: true
  llm_usage_policy:
    max_percent: 20
    trigger: ["unknown structure", "ambiguous clause"]
```

### 2️⃣ **Частная спецификация для 44-ФЗ** (`ldup_44fz.yaml`)

```yaml
document:
  title: "Федеральный закон №44-ФЗ"
  source: "КонсультантПлюс"
  format: "WordML 2003"
  structure_rules:
    chapter_marker: "Глава"
    article_marker: "Статья"
    part_marker: "Часть"
  temporal_rules:
    entry_into_force: "вступает в силу"
    loss_of_force: "утрачивает силу"
  llm_usage_policy:
    max_percent: 15
    review_prompt: "Объясни, как статья регулирует процедуру закупок."
```

---

## 🧠 VI. Интеллектуальный workflow (в цифрах)

```mermaid
flowchart LR
    A[Загрузка документа] --> B[GEPA: структурный анализ]
    B --> C[SIMBA: морфология]
    C --> D[MiPROv2: темпоральность]
    D --> E{Результат полон?}
    E -- Да --> F[Rule-Based Export]
    E -- Нет --> G[LLM Assist (20%) → SRC Training]
    G --> H[Self-Improvement + Cache]
    F --> I[Export → LegalRuleML/AkomaNtoso]
    H --> I
```

---

## 🔧 VII. Требования к реализации

| Категория              | Требование                                                 |
| ---------------------- | ---------------------------------------------------------- |
| **Архитектура**        | Модульная, плагинная, с возможностью отключения LLM блоков |
| **Совместимость**      | Akoma Ntoso, LegalRuleML, MetaLex, LegalDocML-RU           |
| **Форматы**            | XML, DOCX, RTF, HTML, TXT, PDF (через конвертеры)          |
| **Языки**              | Русский (основной), английский (доп.)                      |
| **Token Economy**      | 70–80 % задач — rule-based / алгоритмические               |
| **Обучаемость**        | SRC Loop для самообучения на корпусах актів                |
| **Источники**          | Файловые и веб-источники (HTTP / interactive agents)       |
| **Файловая структура** | YAML спецификации (универсальные + частные)                |
| **Отчётность**         | Журнал по источникам, версиям, редакциям                   |

---

## ✅ VIII. Что это даёт на практике

* 🔋 Экономия до 70 % токенов за счёт rule-first логики.
* 🧩 Полная совместимость с международными стандартами.
* 🧠 Автономное самообучение на корпусе КонсультантПлюс через SRC.
* 🏛️ Готовность к масштабированию на другие базы (Гарант, Кодекс, pravo.gov.ru).
* 🧾 Единый универсальный формат YAML-описания для всех актов.

---

