Отлично ⚙️
ниже — **Mermaid-диаграмма полного жизненного цикла YAML-правил** в LDUP, показывающая, как
📘 декларативные YAML-описания →
🔧 компилируются в алгоритм DSPy Graph →
🧠 верифицируются →
и 🔁 самообучаются через SRC-feedback.

---

## 🧩 **YAML → RuleSpec → DSPy Graph → Verified Algorithm**

```mermaid
graph TD

%% YAML Stage
A1[📘 General YAML<br>(ldup_general.yaml)] --> A3[YAML Aggregator]
A2[📄 Private YAML<br>(ldup_44fz.yaml)] --> A3
A3 --> B[🧩 Schema Validator<br>• проверка синтаксиса и типов<br>• устранение конфликтов general/private]

%% Compilation Stage
B --> C1[⚙️ Rule Compiler<br>(GEPA / SIMBA / MiPROv2)<br>• regex • patterns • morph rules]
C1 --> C2[RuleSpec Object<br>• структура, семантика, темпоральность]

%% Graph Assembly
C2 --> D[🧠 DSPy Graph Builder<br>• создает узлы GEPA/SIMBA/MiPROv2/TCGR<br>• устанавливает зависимости]

%% Execution & Verification
D --> E1[🏗️ Executable Algorithm<br>– структурный анализ акта<br>– морфология<br>– темпоральность]
E1 --> E2[🧪 Verification Layer]
E2 -->|Static| F1[✅ Schema Validation<br>– YAML соответствует модели<br>– типы корректны]
E2 -->|Logical| F2[🔁 Simulation Check<br>– порядок правил и связи<br>– нет циклов и дыр]
E2 -->|Empirical| F3[📊 Corpus Evaluation<br>– сравнение с эталоном<br>– генерация feedback]

%% Feedback & Self-Learning
F3 --> G[🔄 SRC v2 Feedback Controller<br>• анализ ошибок<br>• автоформирование JSONL-патчей]
G --> H[YAML RuleStore<br>• pending rules + new patterns]
H --> I[🧮 YAML Validator + AutoMerge<br>• проверка и одобрение новых правил]
I --> A3

%% Outputs
E1 --> J[🧾 Verified Parsing Algorithm<br>• исполняемый Graph DSPy<br>• совместим с Akoma Ntoso / LegalRuleML]

%% STYLES
style A1 fill:#f7f7ff,stroke:#444,stroke-width:1px
style A2 fill:#f7f7ff,stroke:#444,stroke-width:1px
style A3 fill:#f0f0f0,stroke:#888,stroke-width:1px
style B fill:#fff5e6,stroke:#e6a700,stroke-width:2px
style C1 fill:#e8f5ff,stroke:#3b83f6,stroke-width:1px
style D fill:#d6f9e9,stroke:#2e8b57,stroke-width:1px
style E1 fill:#eaffea,stroke:#2e8b57,stroke-width:1px
style E2 fill:#f5f5f5,stroke:#999,stroke-width:1px
style G fill:#dde3ff,stroke:#0044cc,stroke-width:1px
style H fill:#f3f1ff,stroke:#6246ea,stroke-width:1px
style J fill:#d9f9ff,stroke:#33a1c9,stroke-width:2px
```

---

## 🧠 Объяснение стадий

| Этап                         | Смысл                                              | Реализация                      |
| ---------------------------- | -------------------------------------------------- | ------------------------------- |
| **A1–A3 (YAML Aggregation)** | объединяются общие и частные YAML-описания         | DSPy YAML Loader                |
| **B (Validation)**           | проверка схем, типов, конфликтов                   | `dspy.validate_ruleset()`       |
| **C1–C2 (Compilation)**      | преобразование YAML → RuleSpec (внутренний объект) | GEPA/SIMBA/MiPROv2 RuleCompiler |
| **D (Graph Assembly)**       | RuleSpec → DSPy Graph (узлы, связи, приоритеты)    | `GraphBuilder.from_rules()`     |
| **E1 (Execution)**           | исполняемый алгоритм, анализирует акт              | Rule-based workflow             |
| **E2–F1–F3 (Verification)**  | проверка корректности и точности                   | Schema, Simulation, Corpus      |
| **G–I (Feedback Loop)**      | SRC обновляет YAML через JSONL feedback            | AutoMerge + HCO                 |
| **J (Output)**               | проверенный алгоритм для DSPy и экспорт            | Используется в LangGraph агенте |

---

## 🔍 Верификация происходит на трёх уровнях

| Уровень       | Цель              | Проверяет                                |
| ------------- | ----------------- | ---------------------------------------- |
| **Static**    | Корректность YAML | Схема, типы, структура                   |
| **Logical**   | Логика исполнения | Иерархию, порядок, отсутствие конфликтов |
| **Empirical** | Эффективность     | Совпадение с эталоном (корпус актов)     |

---

## 🔁 SRC и YAML — симбиотическая связь

* YAML хранит формальные правила.
* DSPy Graph применяет их.
* SRC анализирует ошибки и предлагает новые.
* YAML RuleStore принимает, валидирует, обновляет.

➡️ Таким образом, **алгоритм эволюционирует**, а YAML остаётся “человеческой декларацией”, понятной юристу и программисту.

---

Хочешь, чтобы я показал **пример реального цикла SRC-feedback → YAML-patch** на фрагменте закона
(как алгоритм сам добавляет новое правило после ошибки в temporal-обработке)?
